# Cloudflare Pages 响应头配置
#
# 注意：CF Pages 对同名响应头是「拼接」而不是「覆盖」。
# 如果在 /* 里写了 Cache-Control，再在 /*.css 里写一次，
# 结果会变成 "max-age=0, must-revalidate, public, max-age=86400"，
# 浏览器取第一个 max-age，长缓存直接失效。
# 所以 /* 只放安全头，Cache-Control 一律按文件类型单独声明，互不重叠。
# HTML 不声明，走 CF 默认的 max-age=0, must-revalidate（改完即时生效）。

/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=(), interest-cohort=()
  Content-Security-Policy: default-src 'self'; img-src 'self' data:; script-src 'self'; style-src 'self'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; object-src 'none'

# 样式与脚本没有内容哈希，文件名永远不变。
# 一旦被边缘缓存住，就会出现「新 HTML 配旧 CSS」——
# HTML 是 max-age=0 每次都新，CSS 却能停留一整天，页面直接错版。
# （2026-08-31 真实事故：工具子页上线后套用了改版前的旧样式。）
# 注意 Pages 部署只清 pages.dev 的缓存，自定义域名走的是 zone 缓存，不会自动清。
# 所以这里改成每次回源校验：命中 ETag 时只是一个 304，代价可以忽略。
/*.css
  Cache-Control: public, max-age=0, must-revalidate

/*.js
  Cache-Control: public, max-age=0, must-revalidate

/manifest.webmanifest
  Cache-Control: public, max-age=0, must-revalidate

# 图片与图标改动时会连带换文件名，可以放心长缓存
/*.webp
  Cache-Control: public, max-age=31536000, immutable

/*.jpg
  Cache-Control: public, max-age=31536000, immutable

/*.png
  Cache-Control: public, max-age=31536000, immutable

/*.ico
  Cache-Control: public, max-age=31536000, immutable
